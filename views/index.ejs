<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <p>Welcome to <%= title %></p>

    <ul>
      <% databases.map(db => { %>
        <li>
          <a href="<%= db.name %>"><%= db.name %></a>
        </li>
      <% }) %>
    </ul>
  </body>
</html>
